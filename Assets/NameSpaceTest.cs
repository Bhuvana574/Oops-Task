using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using PurpleTalk;


public class NameSpaceTest : MonoBehaviour
{
 
       
    // Start is called before the first frame update
    void Start()
    {
        PurpleTalk.UI.MyUI.DoUI();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
